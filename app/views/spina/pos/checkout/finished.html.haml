- content_for(:title) { "Bestelling afgerond" }

.checkout
  .checkout-container
    = image_tag 'spina/pos/printer.png', style: 'margin: 20px 0', width: 73, height: 74

    - if @change.present?
      .checkout-label Wisselgeld
      .checkout-total= number_to_currency @change

  .checkout-container
    = link_to print_settings_order_path(@order), class: 'button', data: {remote: true, disable_with: 'Printing...'} do
      Print bon
